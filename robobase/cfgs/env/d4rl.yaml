# @package _global_

env:
  env_name: d4rl
  episode_length: 999
  random_traj: false  # if true, will choose the top K (K=demos) trajectories
  stddev_schedule: linear(1.0,0.1,500000)

# demos: .inf # Use all available demos
